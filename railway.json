{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["server/**"]
  },
  "deploy": {
    "startCommand": "[ -d server ] && cd server; node src/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
